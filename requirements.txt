# Full development/test environment.
# Production Docker builds use requirements-runtime.txt.
# Optional dense RAG dependencies live in requirements-rag-ml.txt.

-r requirements-runtime.txt

# Optional dense retrieval / embedding stack:
# -r requirements-rag-ml.txt

pytest>=8.0.0
