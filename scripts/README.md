# Repository scripts

`validate_production_config.py` performs static checks for the production Docker configuration, secret placeholders, and the runtime/optional-ML dependency boundary. It is executed by GitHub Actions.
